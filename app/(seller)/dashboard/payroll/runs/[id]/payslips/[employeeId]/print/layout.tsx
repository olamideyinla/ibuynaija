/**
 * Bare layout for the printable payslip — no navbar or dashboard chrome.
 */
export default function PayslipPrintLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
